#!/usr/bin/env bash
set -e

echo "rubocoppin'..."
bundle exec rubocop -A
echo "erb_lint-in'?"
# Autocorrect mangles Alpine `@click` attributes; use `x-on:click` instead.
bundle exec erb_lint app/views/
