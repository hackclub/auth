Hey <%= @first_name %>,

Your identity verification call is booked<% if @starts_at %> for <%= @starts_at.strftime("%A, %B %-d at %H:%M %Z") %><% end %>.

This call is recorded by default. We record so a decision can be re-reviewed later without asking you to do this again. The recording is stored encrypted, access to it is logged, and it's permanently deleted 30-90 days after a decision is made on your case. If you'd rather not be recorded, let us know at the start of the call and we'll arrange for a second staff member to join instead.

Have your document with you on the call — the reviewer will compare it against what you submitted.

— The Hack Club team
