<%= render Components::PublicActivity::Snippet.new(activity, owner: activity.trackable) do %>
  logged in to <%= activity.parameters[:name] || activity.parameters[:service_provider] %> via SAML.
<% end %>
