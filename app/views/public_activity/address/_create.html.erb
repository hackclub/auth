<%= render Components::PublicActivity::Snippet.new(activity) do %>
  <% if defined?(current_identity) && activity.trackable&.identity == current_identity %>
    added address.
  <% else %>
    added address for <%= render activity.trackable&.identity, capitalize: false if activity.trackable&.identity %>.
  <% end %>
<% end %>
