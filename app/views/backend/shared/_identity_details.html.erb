<%
  show_legal_name = local_assigns.fetch(:show_legal_name, false)
%>
<div class="identity-details">
  <%= render identity %>

  <% if identity.legal_first_name.present? && show_legal_name %>
    <div class="detail-row">
      <span class="detail-label">legal name</span>
      <span class="detail-value"><%= identity.legal_first_name %> <%= identity.legal_last_name %></span>
    </div>
  <% end %>
  <div class="detail-row">
    <span class="detail-label">country</span>
    <span class="detail-value"><%= identity.country %></span>
  </div>
  <div class="detail-row">
    <span class="detail-label">email</span>
    <span class="detail-value"><%= identity.primary_email %></span>
  </div>
  <div class="detail-row">
    <span class="detail-label">birthday</span>
    <span class="detail-value"><%= identity.birthday %></span>
  </div>
  <div class="detail-row">
    <span class="detail-label">phone</span>
    <span class="detail-value"><%= identity.phone_number %></span>
  </div>
  <div class="detail-row">
    <span class="detail-label">status</span>
    <span class="detail-value"><%= identity.verification_status.humanize %></span>
  </div>
  <% if defined?(identity.ysws_eligible) && !identity.ysws_eligible.nil? %>
    <div class="detail-row">
      <span class="detail-label">ysws</span>
      <span class="detail-value"><%= identity.ysws_eligible ? "eligible" : "ineligible" %></span>
    </div>
  <% end %>
  <div class="detail-row">
    <span class="detail-label">slack</span>
    <span class="detail-value">
      <% if identity.slack_id.present? %>
        <%= link_to identity.slack_id, "https://hackclub.slack.com/team/#{identity.slack_id}" %>
      <% else %>
        <i>not set</i>
      <% end %>
    </span>
  </div>
</div>
