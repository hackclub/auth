header "The Taste Test"

section <<~MSG, markdown: true
  _Flavorpheus sets three small bowls in front of you._

  Alright, one last thing. Every cook here knows the secret ingredient. Let's see if you do too.

  *What makes a dish truly great?*
MSG

wrong_answers = OnboardingScenarios::Flavortown::WRONG_ANSWERS.map { |label, action, **opts| button(label, action, **opts) }
terrible_answers = OnboardingScenarios::Flavortown::TERRIBLE_ANSWERS.map { |label, action, **opts| button(label, action, **opts) }
dino_nuggets = OnboardingScenarios::Flavortown::DINO_NUGGETS.then { |label, action, **opts| button(label, action, **opts) }

actions [
  button("Love", "flavortown_taste_correct", style: "primary"),
  *wrong_answers.sample(2),
  *terrible_answers.sample(2),
  dino_nuggets
].shuffle
