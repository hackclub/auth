<% first = @identity.legal_first_name.presence || @identity.first_name %>
<% last = @identity.legal_last_name.presence || @identity.last_name %>
<% verf = @identity.persona_verifications.where(status: :draft).first %>
<% if verf %>
  <div class="persona-name-check" x-data="{ editing: false }"
       style="text-align: center; font-size: 0.9em; color: var(--foreground2); margin: 0.5lh 0;">
    <p x-show="!editing">
      Verifying as <strong style="color: var(--foreground);"><%= first %> <%= last %></strong>
      &middot;
      <a href="#" @click.prevent="editing = true" style="color: var(--link);">not what's on your ID?</a>
    </p>

    <div x-show="editing" x-cloak>
      <%= form_with url: local_assigns[:form_url] || update_legal_name_path, method: :patch, local: true,
            style: "max-width: 22rem; margin: 0 auto;" do |f| %>
        <p style="margin-bottom: 0.5lh;">Enter the name as it appears on your ID</p>
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.5rem;">
          <%= f.text_field :legal_first_name, value: first, required: true, autocomplete: "given-name",
              placeholder: "First name", style: "font-size: 0.85em; padding: 0.35em 0.5em;" %>
          <%= f.text_field :legal_last_name, value: last, required: true, autocomplete: "family-name",
              placeholder: "Last name", style: "font-size: 0.85em; padding: 0.35em 0.5em;" %>
        </div>
        <div style="margin-top: 0.5rem; display: flex; align-items: center; justify-content: center; gap: 0.75em;">
          <%= f.submit "Update", class: "secondary", style: "font-size: 0.8em; padding: 0.3em 1em; width: auto;" %>
          <a href="#" @click.prevent="editing = false" style="font-size: 0.8em;">cancel</a>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
