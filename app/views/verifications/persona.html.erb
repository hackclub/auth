<% @student_id_flow = @verification.is_a?(Verification::PersonaStudentIdVerification) %>
<% sid = ->(key, **opts) { t("verifications.student_id.#{key}", default: t(".#{key}", **opts), **opts) } %>

<div class="page-header-centered">
  <h1><%= t(".title") %></h1>
  <p><%= t(".subtitle") %></p>
</div>

<div class="page-sections">
  <% if @persona_error %>
    <section class="section-card">
      <div class="status-indicator">
        <span class="status-badge rejected">error</span>
        <h3><%= t(".error_title") %></h3>
        <p><%= @persona_error %></p>
      </div>
      <div class="status-actions">
        <%= link_to t(".fallback_link"), verification_step_path(:document), role: "button", class: "secondary" %>
      </div>
    </section>

  <% else %>
    <section class="section-card persona-verify-card"
             x-data="{ editingName: false }"
             data-persona-verify
             data-inquiry-id="<%= @inquiry_id %>"
             data-session-token="<%= @session_token %>"
             data-environment-id="<%= @environment_id %>"
             data-host="<%= @persona_host %>"
             data-status-url="<%= verification_status_path %>"
             data-msg-complete="<%= t(".complete_message") %>"
             data-msg-cancel="<%= t(".cancel_message") %>"
             data-msg-error="<%= t(".error_sdk") %>"
             data-fallback-url="<%= verification_step_path(:document) %>">

      <div class="persona-hero">
        <div class="persona-icon">
          <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="4" width="20" height="16" rx="2" />
            <circle cx="8.5" cy="11.5" r="2.5" />
            <path d="M14 10h4M14 14h2" />
            <path d="M4 20c0-2 2.5-3 4.5-3s4.5 1 4.5 3" />
          </svg>
        </div>
        <h3><%= t(".hero_title") %></h3>
        <p><%= @student_id_flow ? sid.("hero_description_html") : t(".hero_description_html") %></p>
      </div>

      <div class="persona-steps">
        <div class="persona-step">
          <span class="persona-step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="4" width="20" height="16" rx="2" /><circle cx="8.5" cy="11.5" r="2" /><path d="M14 10h4M14 14h2" />
            </svg>
          </span>
          <div>
            <strong><%= @student_id_flow ? sid.("steps.scan.title") : t(".steps.scan.title") %></strong>
            <span><%= @student_id_flow ? sid.("steps.scan.description") : t(".steps.scan.description", document_examples: t(".steps.scan.document_examples.#{@identity.country}", default: t(".steps.scan.document_examples.default"))) %></span>
          </div>
        </div>
        <div class="persona-step">
          <span class="persona-step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="8" r="4" /><path d="M5 20c0-3.5 3.1-6 7-6s7 2.5 7 6" />
            </svg>
          </span>
          <div>
            <strong><%= t(".steps.selfie.title") %></strong>
            <span><%= t(".steps.selfie.description") %></span>
          </div>
        </div>
        <div class="persona-step">
          <span class="persona-step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M20 6L9 17l-5-5" />
            </svg>
          </span>
          <div>
            <strong><%= t(".steps.done.title") %></strong>
            <span><%= @student_id_flow ? sid.("steps.done.description") : t(".steps.done.description") %></span>
          </div>
        </div>
      </div>

      <%= render "shared/verifications/persona_name_check" %>

      <p class="persona-message" data-persona-message></p>

      <button data-persona-start type="button" class="persona-begin-button" x-show="!editingName">
        <%= @student_id_flow ? sid.("begin_verification") : t(".begin_verification") %> →
      </button>

      <div class="persona-footer">
        <p class="persona-fine-print"><%= t(".contact_html") %></p>
      </div>
    </section>
  <% end %>
</div>
